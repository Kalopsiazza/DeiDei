# 第三轮连续批次｜PR #26、#27、#28 验收

ChatGPT · 2026-09-15。验收与下一批设计分别记录。没有合入、关闭 PR、发布或启动新的 Codex 线程。

## 结论

| 对象 | 固定版本 | 结论 |
| --- | --- | --- |
| PR #28，R03-T04-a | 产品 d0c96408a3aa8c14174099da4247bcac953fb9f7；报告 db51a442d0f6640ce499ffbf4320c7098044cb92 | ACCEPTED_LOCAL_INTEGRATION。本机真实联机集成与已验证修复接受；非公网或真人验收 |
| PR #26，R03-T05-a | 产品同 d0c96408；工具 325006a2fd17f9beaa5ee68ae0142da01986806f；报告 e3cd49439eb3e4319ed0c4f9d3cc20a36deff2bf | ACCEPTED_NATIVE_DIAGNOSTIC。两端本机诊断交付接受；保留签名和真实设备项目 |
| PR #27，R03-T03-c | baeff0bf476e1efd91358e9c3bb6350208d8a647 | CHANGES_REQUESTED_TEST_ONLY。测试与旧基线证据保留；Q01 一处错误断言另发 d 包修订，不改产品迎合它 |

第三轮已经形成能在本机运行的真实服务、桌面流程和原生诊断包；第三轮整体尚未完成，仍缺跨电脑受控服务、实际网络与双方设备共同验证。

## 版本差异

独立线程结束时尚未取得 T04 候选，报告的 BASELINE_ONLY/FAIL 对象是旧的 8a6f8b29f517ab4c5a16466f0894e86995f8a852。集成与打包后来采用 d0c96408。这两种报告不能相互替代。

本轮另行使用准确的产品与独立工具分开检出，不改任何产品或提交样本，实际补做最后候选的独立检查。远端比较确认：8a6→d0 只涉及10个代码/测试路径；d0→PR28 head 只有结果材料。核心、runtime、旧规则预期、依赖锁和旧打包不变。

## 规划者实际复测

只读草稿审阅 PR #29 的两个运行：

- [34931393604](https://github.com/Kalopsiazza/DeiDei/actions/runs/34931393604)：固定产品 d0、独立工具 baeff；快速检查与900秒检查分别运行。快速任务保留一次真实失败；持续任务通过。
- [34932084322](https://github.com/Kalopsiazza/DeiDei/actions/runs/34932084322)：同样两个固定输入，补传桌面路径，并用审阅者独立诊断区分限流与重新连接造成的变化。该运行通过。

运行环境为 Linux、Python3.11、Node24.12.0，服务器仅安装固定 hashlock 的 websockets17.0.1。前端沿用原锁，关闭依赖安装脚本，没有运行 Electron 窗口或重做两端二进制。

| 实际检查 | 结果 |
| --- | --- |
| 服务自测 | 66通过 |
| 核心自测 | 174通过 |
| runtime | 23通过 |
| 独立经典规则样本 | 192通过，469次判定；旧会话占位仍单列 |
| 默认桌面检查 | 类型检查、构建、53通过 |
| 独立160房间样本，含实际桌面解码器 | 159通过，N35仍未由该驱动运行，0失败 |
| 独立故障43例原样运行 | 42通过，Q01/rate一项断言失败，见下文 |
| 独立100固定序列 | 100通过 |
| 独立真实钟四房 | 900.058秒，976场，FAIL集合空、违规集合空，退出后连接0和所属任务泄漏0 |
| endurance工具自测 | 2个方法通过；不把作者全部29项改称本轮全跑 |
| 输入文件 | product和independent跟踪文件均未改变 |

第一次160样本调用漏传桌面路径，得到137通过、23未运行；这是审阅命令缺少参数，已保留原输出，随后正确指定桌面后为159通过、1未运行。没有改预期来消除未测。

本轮900秒使用独立线程的四房、每房2玩家1观众程序；不要与作者四房48连接636场的另一份结果相加或混用。这只是该时间与负载下的结果，不宣称任意长期运行都不会出问题。

## 唯一新增发现 F07：测试把重连也当作限流副作用

PR27 的 `tests/rooms_endurance/faults.py:39` 将触发限流前与主动断线、重连、重试之后的 room.seq 直接比较，要求相等。但断线和恢复本来会改变公开 connected 状态并递增 room.seq。旧产品在更前面的空 request_id 检查就失败，未走到这一错误断言。

本轮直接运行原函数，确实在该行失败。独立诊断使用真实 WebSocket，分时记录：

| 阶段 | room.seq |
| --- | ---: |
| 限流前 | 5 |
| 已收到对应 UUID 的限流拒绝、尚未断线 | 5 |
| 服务观察到断线 | 6 |
| 身份恢复 | 7 |
| 原被限流请求重新发送成功 | 7 |

业务命令序号没有被拒绝消耗，原拒绝 ID 可以重试；match 与 policy_revision 未改变。实际产品满足要求。正确测试应在断线前比较副作用，重连后另建比较起点；只检查每一步允许的变化，不简单删除 seq 断言，也不加时间容差。

上述诊断在审阅分支 `docs/reviews/probes/r03_rate_sequence.py`，固定脚本提交11328ac2db85a24ebbf7195acd74ace266b7cd63。它没有修改产品或 PR27 原测试。原43例仍记录42通过+1失败，单独的隔离诊断通过，不能直接把原结果改成43全绿。

## 原生包核验

作者[原生运行34909293225](https://github.com/Kalopsiazza/DeiDei/actions/runs/34909293225)的Mac和Windows job均成功，准确工具325006a2、准确产品d0。读取了成包驱动、真实自动化记录与截图；各端5场联机、10次开退，以及服务持续停止后的离线单人均有对应记录。这些窗口操作来自执行线程，不声称本轮重新操作了两个系统。

本轮实际下载两个最终artifact并校验：

| 平台 | 内部ZIP | 字节 | SHA256 |
| --- | --- | ---: | --- |
| Mac arm64 | DeiDei-R03-T05-a-CANDIDATE-darwin-arm64-d0c9640.zip | 149937759 | 4409f0d016adb91405d4e367487e18fabc5cc5cd634d9abe2f40fa8102959d21 |
| Windows x64 | DeiDei-R03-T05-a-CANDIDATE-win32-x64-d0c9640.zip | 178846599 | 80e185cec1c05817bee31019733480693ae8d8888d52c6cf419a0f2b87a99e27 |

两层哈希、FILE-MANIFEST、重复位置的build-info、source/tool双SHA、stage输入值及后台架构核对相同；Mac341项含327普通文件和14链接，Windows250普通文件。Mac链接目标及ZIP权限另外检查。240个产品blob、145个独立测试及证据blob与取回的Git树相同。未执行跨平台二进制。

Mac仍未公证且Gatekeeper拒绝，Windows未签名；当前包仅含本机回环服务，无法据此称为异地好友可用。新版真人、跨电脑、物理断网、干净机与系统信任未在本轮通过。上一轮Mac人工通过不能自动覆盖这份联机包。

## 接续决定

F07只交给独立测试线程修；产品主线继续做远端安全连接的本地准备，暂不采购或部署。两份包分别写明输入与程序设计，无新玩法或美术问答。产品 d0 和包工具325006a2继续作为当前通过的准确版本；所有现有PR仍未合入。
