# 第二轮首批交付｜规划者验收

ChatGPT · 2026-09-13。接受的是下列固定版本及所述范围；原 PR 均未合入。

| 对象 | 固定提交 | 本轮判断 |
| --- | --- | --- |
| R02-T01-a / PR13 | 529d9b5369d03998355df079095f8b1a317be0ee | ACCEPTED_CORE：纯规则核心可供后续集成 |
| R02-T02-a / PR14 | 3817472f5b199151128a6631bd64cee1d2ed0649 | ACCEPTED_TEST_SUITE：独立样本与驱动可供持续验证 |
| R02-T03-a / 远端分支 | b65842a8e2fcaebf0ddef74c4f3cf4ca5aa366d1 | PARTIAL：接受原型为集成输入，保留下述修订和分发风险 |

## 本轮真实联测

为避开工作环境下载仓库失败，在独立只读权限审阅分支创建了草稿 PR15。它把两个固定版本分别取到不同目录，不修改核心或预期，不使用旧引擎替代。

[第一次执行](https://github.com/Kalopsiazza/DeiDei/actions/runs/34709718426)，第二次执行34709853840增加了桌面源码和已有截图的只读取回。两个运行都成功，环境为Linux / Python3.11.16。结果：

- 核心自测174项通过；样本工具自测8项通过。
- 独立样本194份覆盖82组、33入口、205份回合预期。
- 真正对核心执行192份样本，469次resolve_round调用，失败0。
- C074/room_state_preserved 与 C081/session_request_replay 两份会话样本未运行，不能算作194全通过。

将CI产物下载、核对SHA后，本地Linux / Python3.13.5再次运行同一独立驱动，192份仍全部通过。主源码四份Git blob与远端固定文件相同。

## 测试能否识别错误

在临时副本分别加入五个准确的错误，每次先通过语法和导入，再运行原封不动的独立样本。原核心与样本的文件校验值前后相同。

| 临时错误 | 被识别的失败样本数 |
| --- | ---: |
| 本轮受致命作用后跳过该人的后续行动 | 8 |
| P3错误恢复攻击对撞防御 | 20 |
| 张新伟被额外扣1DD | 2 |
| 曾义休整被算成历强重复目标 | 2 |
| 同一目标只把击杀记给第一名作用者 | 19 |

五项均为行为检查失败，没有以语法错误充数。这只覆盖这五个具体错误，不能称为全部可能缺陷的证明。奖励旧请求重放需要真实会话，仍未运行。详细日志及复现脚本在本轮交接附件的evidence目录。

## 桌面审阅

通过固定分支取回源码、47份清单产物并逐项核对hash；CI省略的.gitignore另从同一提交读取并核验。读了主进程、本机档案、演示数据、键位、renderer和测试程序，查看了作者真实截图。没有在本轮环境启动Electron；Mac9项自测与窗口自动化结果仍标明来自执行线程。

本轮未找到桌面对应PR，但远端分支已经存在。REPORT中“未push/未创建”的交付状态需要执行线程修正，不能据这段旧文字否认已上传代码。

集成前处理：

- U01：对手席位只画了昵称、头像、提交状态，没有显示其公开资源。DD与主要资源应在对应席位可读，不能只给本人显示。
- U02：DD显示只为0、36做了特判，其他值显示raw/6。改成精确的通用分数格式，保留大整数精度。这是显示整理，不改变规则数值。
- U03：getView轮询只处理ok，失败响应被忽略、Promise拒绝未显式接住；尚未提交或强制休整时也不轮询。真实会话接入时要覆盖读取失败、自动休整、晚到响应和退出后的旧响应。
- U04：当前标题、结果和手册提示固定写演示；真实模式根据source=live显示，开发预览继续明确fixture。不能把脚本结果换个标签冒充实算。

UI三排十一列与三类18/9/6保持。图形仍是占位，当前不做新IP或动画。23项开发工具链告警继续保留；不以生产依赖audit为零宣布整体安全。此轮允许本地源码集成，未允许Forge分发、安装包发布或绕过系统防护。

## 之后怎样推进

新建integration/r02作为隔离的集成起点，三份代码目录沿用上表准确tree，原PR和main不受改变。桌面仍有修订项，所以这个起点不代表成品通过。

由ChatGPT编写PRD、架构和R02-T04-a，Codex在自己的任务分支实现本地会话、worker、真实单人循环与以上显示修订。先不做联网、训练、正式动画或分发。独立会话复核与分发工作随后另发包。
