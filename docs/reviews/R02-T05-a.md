# R02-T05-a｜PR #19 验收

ChatGPT · 2026-09-14（Asia/Shanghai）。

## 结论

**ACCEPTED_INTERNAL_DELIVERY：接受本包的实现、两端内部运行包和已有测试证据。** 本轮未发现需要再发代码修订包的问题。macOS 常规人工流程采用 Teddy 在执行线程里的整体通过确认；Windows 真人、干净电脑以及系统信任项目仍未完成。因此，完整跨设备验收继续为 PARTIAL，不把所有 P 项改成通过，也不宣称正式发布通过。

第一轮已完成进入开发所需工作；第二轮的纯规则、源码单人及两端自带运行时的内部包已经交付。下一步可以准备第三轮房间 PRD 与架构，不等待自愿协助同学的交期；Windows 等未测事项继续跟踪。当前没有新发第三轮执行包。

## 固定来源

- PR #19：https://github.com/Kalopsiazza/DeiDei/pull/19
- 受审 head：`23520350393ba43384c20b60bef7d6dbd89fc142`。
- 实际成包与执行者受测代码：`45dbf9c4e13e693b01736a79cb97652f760f8bad`。
- 产品输入：`3ce99f1cdd5f2f52c757ef61b696ecaade4877d0`，即 PR #18 已通过版本。
- 原规划附件：deidei-r02-t05-handoff.zip，规划清单 SHA256 为 `84f621cc6a20739c6836a83bb5133659c8fbbcdebaef585a69033aa86fd498fc`，plan_sha=null。本文按原 PRD、架构及 P01—P18 逐项审阅。

远端比较显示，产品输入到成包提交有 63 个变化路径，位于获准的打包、启动定位、资源定位、新测试、说明及具名工作流范围。大量行数来自许可证与依赖清单，不能当作重新编写游戏的工作量。核心、独立规则预期、Session/solo/opponent/worker 业务、卡牌资料、既有 CI 和旧模型未改。包内 build-info 记录的 105 份制作输入哈希与受审 head 取回文件全部相同。

## 实现检查

已读 worker-launch.cjs、worker-port、resource_paths.py、PyInstaller spec、pack.mjs、build.py、最终包自动化脚本和 CI。

成包由主进程传入 isPackaged/resourcesPath，后台采用包内绝对路径；清除本子进程的 Python/venv 定位变量，不调用系统 Python。后台和两份资料缺失时显示 PACKAGE_INCOMPLETE，不寻找旧源码、不下载替代品、不退回演示。源码启动保留原用法。

Python 后台使用指定 onedir 方式并保留 stdin/stdout，Windows 父进程隐藏控制台。最终资源、后台、手册、许可和应用元信息均包含在交付文件内。旧模型、开发 node_modules、测试专用启动脚本和字体文件未进入成品清单。

## 规划者独立复测

[只读复测 run 34773625634](https://github.com/Kalopsiazza/DeiDei/actions/runs/34773625634) 固定受审 head，使用 Linux、Python 3.11.16、Node 24.12.0、npm 11.6.2。沿用候选 package-lock，安装时关闭安装脚本；本次未下载或运行 Electron 二进制，也未在 Linux 伪造 Mac/Windows 成包运行。

| 检查 | 实际结果 |
| --- | --- |
| 纯核心自测 | 174 通过 |
| 运行层 | 23 通过，含原 21 与新增资源定位 2 项 |
| 独立核心样本 | 192 份通过，469 次判定调用，失败 0 |
| 独立工具自检 | 8 通过 |
| 默认 npm test | 类型检查、构建和 25 项测试通过 |
| 根检查 | 39 通过，1 项原有 expectedFailure |
| 新查 npm 全依赖审计 | 0 条告警，退出 0 |
| 新查 npm 运行依赖审计 | 0 条告警，退出 0 |
| 跟踪源码前后检查 | 未改变 |

原独立驱动两份会话占位仍为 NOT_RUN；历史另有六项规划者会话检查，不混记为 194 份全通过。

旧 23 条 npm 告警的链路退出与当前依赖清单相符。上游 PyInstaller 公告 GHSA-9fxf-4qw3-ghmr 的修复版本为 6.22.1，本包使用 6.22.3；该点另经上游页面核对。一次 npm 检查与部分公告核对不能证明 Electron、Python 及整个二进制没有任何漏洞。本轮没有进行完整二进制 CVE 扫描。

## 交付文件的独立核验

我实际下载了两个最终 Actions artifact，分别核对外层 ZIP、内部应用 ZIP、build-info 与包内文件清单。两个外层 digest 与平台元数据相同，两个内层的大小和 SHA256 与报告相同。

| 平台 | 实际内部 ZIP | 大小 | SHA256 |
| --- | --- | ---: | --- |
| macOS arm64 | DeiDei-R02-T05-a-macOS-arm64-45dbf9c.zip | 139460469 字节 | `00cf51f3571ac8d562d6d0409c6c2f6f28b308eedf77ccbfc925a969af873c64` |
| Windows x64 | DeiDei-R02-T05-a-Windows-x64-45dbf9c.zip | 168641592 字节 | `6439fe9d2de06bc76641747bc9acbd51b1a933130f271031bbb8351e5aefd1f1` |

Mac 清单内 309 个普通文件及 14 个符号链接，Windows 清单内 170 个普通文件，逐一核对内容和大小；Mac 另外核对 ZIP 记录的权限与链接目标。Mac 归档的 __MACOSX 元数据单独排除；它的原生 UTF-8 文件名使用正确编码读取，不因通用 ZIP 读取器默认编码把使用说明误报成缺失。包内六份主要 CJS 与受审源文件相同，catalog/entry-map 与源字节相同，后台架构分别为 arm64 Mach-O 和 x64 PE。每端 build-info 的 105 个输入值均核对通过。

取回审阅源码与证据的 artifact 为 10322782152，SHA256 `c5ce7caa8e2285ecb310b8c224f50eb3361a066b1cab4345f17d0efc5b6cf85b`。与 Git 树核对了实际取回的 404 个 blob；执行线程清单内 204 份证据文件全部通过大小/哈希检查。

原生运行来自[候选构建 run 34771885020](https://github.com/Kalopsiazza/DeiDei/actions/runs/34771885020)。已核对两端 job 成功，并阅读原始命令和 packaged-automation.json：每端 5 场真实随机对局、再开、10 次退出回收、缺文件、开发路径隔离、分数 DD 和自动休整都有对应断言。脚本从最终 ZIP 展开的实际应用启动，isPackaged=true，使用原生系统账户及真实后台；没有在产品中插入 set_state 或测试种子。

本轮查看了两端成包截图；这些仍是执行线程原生 CI 的应用画面。我的环境只完成源码测试与包内容核验，未本地执行两个跨平台二进制，未重跑 Mac/Windows 图形操作或签名验证。

## 人工与剩余项目

Teddy 在收到 Mac 常规流程后回复“都没问题， macOS验收通过”。原记录见固定 head 的 docs/results/R02-T05-a/HUMAN-ACCEPTANCE.md。接受该整体反馈；流程含物理断网，但没有逐项日志和计数，因此不推断额外场次、新机器参数或新档案离线首启。用户没有单独回传包哈希，包对应关系沿用当时提供的版本上下文。

- Windows 普通用户真人启动、只读应用目录、实际系统安全提示、物理断网与笔记本显示仍未测。Windows Server 2022 的原生 CI 通过不替代同学的笔记本。
- 两端无开发工具干净电脑、新档案首次离线的专门检查仍未单独确认。PATH 隔离是技术验证，不记作干净机通过。
- Mac ad-hoc 完整性检查通过，但原生 CI 的 Gatekeeper 返回 3/rejected；未公证。Windows 未签名。保留 P16 原始 FAIL/NOT_RUN，不将人工能打开改写成公证或系统信任通过，也不以本机既有 security disabled 状态作为通过依据。
- 原计划已允许 Windows 协作延期。上述设备缺项不影响第三轮的设计准备，但第二轮的完整跨设备交付仍未全部完成。

本次没有提出新的 F 类代码修订。后续 Windows 实测采用这份新版最终 ZIP；第一轮旧实验包的通过不能代替新版结果。现有 Windows 包 artifact 10322059034 实际到期为 2026-10-13T17:34:43Z，Mac 包 artifact 10321614908 到期为 2026-10-13T17:34:31Z。签名策略在更多人分发前单独处理，不关闭系统防护。

## 接续

第三轮下一个主题为好友房详细设计：房间建立/加入/准备、2—6真人与观众、共同出牌计时、连续三轮缺席、房主离开与网络中断。ChatGPT先写 PRD 和架构，再发编号执行包。尚未确认的观众上限、短暂失联等待、出牌时间等不由执行者猜定。

本次仅完成验收与计划维护，不合入/关闭 PR #19 或旧 PR，不修改 main，不发布 Release，不启动新 Codex 任务。实际产品保留在 PR #19 候选分支；integration/r02 的文档更新不等于产品代码已合入。
