# R02-T04-a｜规划者验收

ChatGPT · 2026-09-13。受审为 PR #16 的 `980a019d9a1410bf3ee72263d90397023696f32b`。

**结论：CHANGES_REQUESTED，仅需完成本文件 F01、F02 两处修订。真实单人、会话与通信主体已通过本轮所列复测。** 不改经典规则，不重写已通过的核心。本结论没有合入或发布含义。

## 实际证据

- 远端比较确认：相对输入4102921，59个变更路径都在game/runtime、game/desktop和本包结果内；核心、独立样本、锁文件和CI未改。执行线程测试提交e340a2a到受审980a019的差异仅是结果资料。
- [本轮只读复测](https://github.com/Kalopsiazza/DeiDei/actions/runs/34730430608)：Linux/Python3.11.16，核心174、runtime13通过；独立核心192份样本通过，469次调用，失败0。原驱动的2份session条目仍未改动。
- 原资料的34份清单文件全部核对大小与SHA256。取回包SHA256为5f1e4c5c9e322ffd040972bf540f386f5e64e5c62f604711099f5bd36f72beff。session.py、solo.py、worker.py本地Git blob与远端一致。
- 本地Linux/Python3.13.5再次运行runtime13项通过。独立新增6项会话检查通过：C074开局人数/模式/缺席信息保留；C081到账重试、消费后的旧请求、不同载荷、缓存移除后的过期请求；无效提交和关闭重开。旧的2份session样本没有被改成PASS，六项结果另列。
- Node22.16.0下，原桌面9项加新通信9项共18项通过。为运行这些单元测试，仅用本机TypeScript5.8.3临时转译三个非UI模块；没有运行指定版本的完整类型检查、esbuild构建或Electron窗口。本地临时输出不提交生产目录。
- 作者提供的12条窗口账目重新交给准确核心计算，全部相同；包含3场记录，其中2场完整结束。这是账目复算，不是本轮重新操作Mac。
- 查看了实际提交的1366牌桌、揭晓等截图。Windows、Mac真人、真正断网、安装包、签名和模型本轮未测试。23项既有开发依赖告警仍在，未重新审计。

## F01｜P2｜被取消的攒显示成资源增长

文件：game/runtime/deidei_runtime/view.py:80-85。

从零开始，A攒、B云。核心正确给A余额0；攒事件result=suppressed、resource_delta=6。当前摘要仍写“玩家A：DD 6（六分之一单位） · resource_gain”，省掉了“未生效”。资源数字算对，文字却让人以为获得资源。

已用当前代码调用真实核心与ledger_summary复现。修订先检查event.result：只有applied展示实际变动；取消的攒说明本次未增加DD。DD显示为玩家单位，使用中文动作说明，不改账目、原始事件或规则。

## F02｜P2｜开场未完成时返回，晚到响应又把人带回牌桌

文件：game/desktop/renderer.tsx:62-69、90、96。

准备页点开始，startSolo尚未返回；此时“返回”与顶部标题仍能跳到菜单，且没有让开场请求失效。开场完成后changeScene仍执行setPage('table')，页面又回到牌桌。旧场的轮询返回已被处理，这条开场导航路径尚未覆盖。

本轮提取候选源码里的准确changeScene与返回处理函数，使用延迟Promise运行，得到prepare→menu→table。属于处理函数级复现，未声称Electron实机复现。

本次选定修法：开场/切换场景期间暂时禁用准备页返回、顶部返回和其他可离开当前页面的入口；处理函数同样读取当前转换标志。完成或失败后恢复操作。不添加新的开场取消协议。窗口关闭仍沿既有确认/回收路径处理。

## 本轮收尾

只发[R02-T04-b](../tasks/R02/R02-T04-b.md)。原任务04线程完成两处修订及相应测试；把test-live.cjs纳入默认npm test，使后续标准检查覆盖已有通信回归。相关PRD和架构由ChatGPT编写。

主体可以保留。源码可玩验收待这两处修订回传后结束；第二轮不因同学Windows任务延期而停等。联网、完整动画、训练、正式分发仍另行安排。没有创建新的开发执行会话，没有合入PR #16。

本轮本地日志、6项独立测试源码、处理函数复现、JSON证据在deidei-r02-t04-b-handoff.zip内的review-evidence目录。它们不冒充原线程产物。
