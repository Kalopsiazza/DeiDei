# 第三轮｜任务04｜a｜真实联机集成与自动回归

ISSUED · ChatGPT · 2026-09-15。建议原服务线程执行；一个线程同时负责服务和桌面联调，避免双方相互等待。

先读../../production/R03-AUTOMATED-START.md、../../prd/R03-AUTOMATED-v1.md、../../architecture/R03-AUTOMATED-v1.md及案例表。旧rooms-1.1正文在附件。产品起点8a6f8b29f517ab4c5a16466f0894e86995f8a852，分支codex/r03-t04-a-live-integration，结果目标integration/r03-live。

## 连续完成，不逐步等人

1. 核对三目录tree及core/runtime/npm锁，复跑原独立160例，保存首次输出；原作者的156/3/1不能当作本轮运行。运行服务/桌面各自检查。
2. 按A02/A03修限流回执与稳定截止；增加自己的服务回归；完整159可执行样本及真实时钟失败例连续三跑。既有输入若还有失败，按A05修已定义行为，不改独立预期。
3. 把service、NetworkRoomPort、preload与真实Electron连起来。默认npm test含原测试及在线测试；新增game/integration/run.py（或等义清楚的入口）管理进程、临时档案、命令和返回值。
4. 实际三个Electron房主/玩家/观众，再用脚本socket填满6+6；执行Q05—Q17和Q23。至少三场完整结果并回大厅再开。再验证原单人、档案与错误恢复。禁止调用MOCK驱动作为真联机截图。
5. 不等待独立线程就运行至少30个固定种子的房间序列与一次真实钟持续运行；收集协议/状态/渲染的错误，按允许范围修并重跑。原桌面、核心、runtime全量再跑一次。
6. 代码达到A10条件后提交code_sha，立即另交CANDIDATE.json并推送、创建草稿PR，让其他两个线程能自行取固定版本。此时继续完成剩余真实窗口和结果整理，无需Teddy回复；最终更新同PR说明。

允许：game/server/**、game/desktop/**、game/integration/**、docs/results/R03-T04-a/**。package.json只改脚本；依赖版本与lock、game/core、game/runtime、两套原独立预期、game/packaging及CI不动。测试专用辅助不得进生产路径。

找不到图形环境时继续真实socket和源码回归，交PARTIAL；不能因截图要求停下全部开发。明确的规则/新产品问题写PENDING-DESIGN，默认不问正在休息的用户。实现自身错误在本包修，不自动发c包。

## 交付

REPORT、TEST-MATRIX、MANIFEST、CANDIDATE、PR-BODY、首次失败/修后输出、真实窗口截图、运行命令、实际退出码、两个模式回归。N35与自己新GUI证据区别说明。CANDIDATE包含base_sha/code_sha/core_tree/runtime_tree/desktop_lock_sha256以及几个阶段真实状态；GUI未测如实标记。

候选在指定分支；发布后不要强推掉已经消费的提交。最终产品有新修订则再发布新code_sha和回执，保留旧记录。最多两次供其他线程消费的候选，测试未全过时只交失败报告、不标可打包通过。

Git：在新的目录git worktree add -b codex/r03-t04-a-live-integration <new-path> 8a6f8b29f517ab4c5a16466f0894e86995f8a852。逐路径stage、git diff --cached --check、提交[R03-T04-a]；push -u origin codex/r03-t04-a-live-integration；gh pr create --repo Kalopsiazza/DeiDei --base integration/r03-live --head codex/r03-t04-a-live-integration --title "[R03-T04-a] 真实联机集成与自动回归｜提交验收" --body-file docs/results/R03-T04-a/PR-BODY.md。fork使用用户名:分支。回读成功才称已提交，不合入原PR或发布。
