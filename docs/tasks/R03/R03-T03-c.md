# 第三轮｜任务03｜c｜独立长期运行与故障验收

ISSUED · ChatGPT · 2026-09-15。继续原独立测试线程。已有b包找到真实问题，保留发现和预期，不为让产品变绿降低断言。

先读本批总入口、PRD、架构A02/A03/A07/A10、Q01—Q24。产品起点8a6f8b29f517ab4c5a16466f0894e86995f8a852，已含准确b服务/桌面/160样本。工作分支codex/r03-t03-c-endurance；目标integration/r03-live。

## 连续工作链

1. 原样跑160例和11个交叉场景，记录实际失败；固定服务、桌面、工具的SHA/tree，保留N33/N36/N48。检查上次a→b预期修改都有规范理由。
2. 增加Q01—Q05的独立检验：正确限流关联、不消费last_seq、wall改变不影响mono、同seq准确deadline，以及当前/未来时限。不要仅检查1ms容差。可对公开行为做参数化，不修改产品。
3. 在tests/rooms_endurance实现Q10—Q20：故障序列、只读观察者差分、重连重放、同房竞争、writer队列精确预算。每个失败输出最小可复现种子与阶段，记录安全字段。队列内部探针与真实socket慢读分开说明。
4. 默认100个固定seed序列，加900秒/最多四房的真实钟运行；每房至多12连接。关闭后检查自己创建的连接/任务和子进程是否回收，报告时延与内存实际样本，不预设“性能达标”。可以先对已有基线完成这些脚本和不依赖缺陷的检查。
5. 按总入口最多三次检查T04的CANDIDATE.json，取得后检出准确code_sha，只读运行原160例、新故障/长期用例和真实server→desktop解码。不能从浮动本地未提交代码取产品。没有候选则交BASELINE_ONLY、当前失败与明确复跑命令，不干等。
6. 在自己的临时副本中对五类错误做缺陷注入检验测试敏感性：空限流ID、期限每次重算、丢掉membership通知、重复应用请求、观众带上别人的待出牌。先语法/导入通过，再行为断言失败；不把语法错误当检测能力。副本不进产品提交。

允许tests/rooms_v1/**、tests/rooms_endurance/**和docs/results/R03-T03-c/**。保留原160个case_id；原基线问题只在明确测试工具缺陷时修改，并说明原/新预期来源。core/server/runtime/desktop/packaging/CI及锁只读。禁止顺手修服务。

所有测试只打自己启动的127.0.0.1/::1服务。最多48连接、900秒一次持续测试、两份T04候选；失败可局部复跑，不做无限压测。Windows和GUI没有环境就未测。无需联系用户、买服务或改防火墙。

## 交付与同步

交REPORT、FINDINGS.json、TEST-MATRIX、MANIFEST、PR-BODY、全部命令/退出码、第一次失败与最新固定版本结果。FINDINGS只含issue_id/severity/source_sha/expected/observed/复现步骤/建议涉及文件，不带token/密码。找到阻止主线的问题尽早push结果，T04可据证据修自己的产品。

测试交付是否完成与产品是否通过分开。写明160例的PASS/FAIL/NOT_RUN，不虚报159socket+若干GUI为160全过。原N35如仍不由此驱动实际执行，保留未测。

Git：新worktree从8a6f8b29f517ab4c5a16466f0894e86995f8a852创建codex/r03-t03-c-endurance；只stage获准路径，git diff --cached --check；提交[R03-T03-c]、push；gh pr create --repo Kalopsiazza/DeiDei --base integration/r03-live --head codex/r03-t03-c-endurance --title "[R03-T03-c] 独立长期运行与故障验收｜提交验收" --body-file docs/results/R03-T03-c/PR-BODY.md。回读base/head/SHA，不合入、部署或发布。
