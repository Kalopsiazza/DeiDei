# 第三轮｜任务05｜a｜原生诊断包与成包联机验证

ISSUED · ChatGPT · 2026-09-15。建议原打包线程；与T04并行准备，产品文件不由本线程修改。

先读本批总入口、PRD、架构A08—A11与Q21—Q24。产品基线8a6f8b29f517ab4c5a16466f0894e86995f8a852。工作分支codex/r03-t05-a-native-diagnostic；目标integration/r03-live。

## 无候选时先做这些

1. 读取R02成功打包代码、现在线main的require链及service入口，列客户端/服务的精确stage清单、许可、固定版本与包内路径。旧打包stage未包含online模块，不能直接把原命令成功当在线包完成。
2. 在game/packaging_r03制作独立构建脚本：--source指只读产品检出，--output为本次新目录；build-info同时记录source_sha和packaging_sha。冻结room-server onedir，不改既有CLI公开能力；客户端仍带原离线worker。
3. 生成两端独立hash lock，只加入现有固定websockets17.0.1；npm版本/lock不改。制作工具自检：缺online模块/资源、错误体系架构、包内测试文件泄漏、代码SHA不符、损坏清单、目录空格及错误产物路径。无需等待T04即可完成。
4. 新增唯一获准CI .github/workflows/r03-native-diagnostic.yml，按A09只读/具名/固定来源。可以在基线做构建诊断，但保留已知产品失败、产物标DIAGNOSTIC_BASELINE，不作为通过版本发给玩家。

## 候选到达后自动继续

5. 按总入口取T04具名回执，验证候选后在临时目录打包；原线程源文件不修改、不复制到自己PR。用提交内candidate-input.json固定source_sha并在每个runner核验，不用未提交或浮动HEAD。
6. 最终ZIP重新展开、启动独立冻结server（port0）和真实成包Electron，再启动正常协议测试peer。每端至少五场、十次退出/重开；实际观测改时限、生效时点、普通移除/房主离开/重连失败，原离线单人仍可玩。测试过程完全自动，MOCK只做工具自检，不计此步骤。
7. 核验内外hash、文件与符号链接、可执行位、许可、OS/CPU、源版本；按原生平台实际签名检查记录ad-hoc/未公证、未签名提示，不改变系统安全设置。保留原生CI与真人/干净机的区别。
8. 将两端产物上传具名内部Actions artifact，记录真实ID、SHA、有效期、下载和启动说明。应用二进制不进Git。没有适用native环境时交已完成脚本、单平台结果与精确复跑命令。

允许game/packaging_r03/**、.github/workflows/r03-native-diagnostic.yml、docs/results/R03-T05-a/**。game/server、game/desktop、game/core、game/runtime、旧game/packaging和旧CI只读。若产品缺少运行所需文件引用，提交具体文件/错误给T04，自己不改main或renderer。

构建依赖仅沿已验证的版本；执行时重查实际审计，适用高/严重未处理则相关包停止交付，不能用audit fix或跳过检查通过。工具安装只在专用工作环境。CI写入、登录或系统权限被拒就记录，不改写旧CI绕过。

不依赖用户提供云主机、域名、证书或公网IP。本批服务只在本机运行，诊断包不能宣称朋友可异地联机。无需唤醒Teddy；人工系统提示等放PENDING-HUMAN。

## 交付与PR

REPORT、TEST-MATRIX、MANIFEST、BUILD-INFO示例、PR-BODY、CANDIDATE-INPUT、下载清单、工具和实际成包日志。区分baseline诊断和修订候选，缺图形时不记PACKAGED_AUTOMATION_PASS。

每平台最多初次构建加两次有实质修复的重试；没有候选时先完成工具和基线诊断，最后交下一条命令，不整夜等待。成果中不给整个仓库新增许可证，不打包私人档案或字体。

Git：新worktree从8a6f8b29f517ab4c5a16466f0894e86995f8a852建立codex/r03-t05-a-native-diagnostic。逐项stage，git diff --cached --check；官方原文换行若有告警另记，不能改原文掩盖；提交[R03-T05-a]并push。gh pr create --repo Kalopsiazza/DeiDei --base integration/r03-live --head codex/r03-t05-a-native-diagnostic --title "[R03-T05-a] 原生诊断包与成包联机验证｜提交验收" --body-file docs/results/R03-T05-a/PR-BODY.md。回读base/head及SHA；不发布Release、不合入或关闭旧PR。
