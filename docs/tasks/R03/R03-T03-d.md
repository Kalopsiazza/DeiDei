# 第三轮｜任务03｜d｜独立最终候选复核与Q01修订

作者ChatGPT · 2026-09-15。建议原独立测试线程继续。不问产品选项，不做图形选择，不修改产品。

## 固定输入

- 本工作分支从PR27 head `baeff0bf476e1efd91358e9c3bb6350208d8a647` 新建 `codex/r03-t03-d-final-candidate`。
- 外部只读产品为 `d0c96408a3aa8c14174099da4247bcac953fb9f7`，另建临时干净检出；不要用本测试分支里旧的产品目录代替。
- 原规划a1e01ee与本批plan/r03-review-next-v1只读，完整plan_sha见转交文字。先读本批PRD、架构A01和D01—D05、验收报告F07。
- 结果PR目标 `integration/r03-live`，不改原PR27或其历史报告。

## 连续工作

D-a：校验两个准确SHA及core/runtime/锁文件未变，原Q01在最终候选复跑一次，保存准确失败行。先确认与验收报告一致，不根据旧基线FAIL判断新产品。

D-b：按照架构A01修正观察时点：限流前后在不断线窗口内比较；有意断线/恢复另记录connected与room.seq；原限流UUID重放成功。保留全部业务与精确时间断言。给修订的工具增加正负自检。

D-c：在最终候选运行原160房间样本，显式给--server-path、--core-path、--desktop-path、准确SHA；159可执行全部通过，旧N35仍单列。新43故障、100seed、旧核心192样本与工具回归分别执行。已有规划者最终900秒记录可核对后引用，不伪称自己再次运行；工具改动影响长跑时最多再跑一次900秒。

D-d：五类变异以最终候选作正控，在临时副本中分别证明对应测试能检出错误。测试输出不能删减或改成产品实际输出。正控失败就写BASELINE_FAIL/工具问题，不计KILLED。临时副本不能进入产品分支。

D-e：将曾用旧基线的结果与新候选结果分别放置；生成candidate-recheck.json、FIRST-FAILURES.md、REPORT.md、MANIFEST.json、COMMANDS.json。更新可复跑命令，提交结果PR，不等待另一个线程。

## 允许修改

仅tests/rooms_endurance/**、为修订工具配套的tests/rooms_v1/**、docs/results/R03-T03-d/**。160原case_id及玩法预期不变；仅有明确前置/驱动缺陷才修测试工具并记录理由。禁止改game/**、依赖锁、CI、正式规划、旧结果。未知产品故障记录FINDINGS.json，继续无关用例，不自行改产品或发下一包。

## 环境与提交

使用独立venv安装产品现有hashlock的websockets17.0.1，Node用原24.12.0；缺工具/权限记未测，别安装系统级环境。最多48连接，测试仅自己创建的回环服务，进程/临时档案自有。

分支从固定输入开始：`git fetch origin <input_sha>`后`git worktree add -b codex/r03-t03-d-final-candidate <新目录> <input_sha>`。只stage允许目录的明确文件，`git diff --cached --name-only`核对后提交，推送同名远端分支，以`[R03-T03-d] 独立最终候选复核｜提交验收`向integration/r03-live建PR。没有push权限可用自己的fork，base不变，禁止借用用户凭据。

报告分别写工具SHA、产品SHA、规划SHA、实测和引用证据。先提交工具代码再实际测试，结果提交不可冒充受测工具版本。用户转交授权该分支的提交与PR，不授权合入、发布或改任何已提交原结果。
