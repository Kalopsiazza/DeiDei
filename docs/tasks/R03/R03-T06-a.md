# 第三轮｜任务06｜a｜安全远端连接准备与本地TLS联测

作者ChatGPT · 2026-09-15。建议原任务04集成线程继续。一份包含实现、故障检查、真实窗口验证和部署资料的较大包；不需要画风或玩法问答。

## 固定输入与设计

产品从已验收 `d0c96408a3aa8c14174099da4247bcac953fb9f7` 新建 `codex/r03-t06-a-secure-preparation`。当前integration/r03-live只是规划与旧起点，不作产品输入。

先读本批[PRD](../../prd/R03-NEXT-v1.md)、[架构](../../architecture/R03-NEXT-v1.md)、[S01—S18](../../contracts/r03/NEXT-CASES-v1.md)，规划固定plan/r03-review-next-v1，完整SHA见转交文字。核心classic-1.0.1和rooms-1.1字段不变。Codex不另做技术选型或正式PRD。

## 连续完成的步骤

T-a：核验准确输入和只读目录。运行已有server66、desktop53及核心/runtime/独立规则；记录本机环境，保存已有失败。准备新的专用测试环境。

T-b：实现架构A02的可选TLS启动、成对证书检查和remote许可校验。默认仍回环明文，旧testing工厂保持兼容。只在回环端口实测，非回环仅做配置函数测试。

T-c：实现A03/A04的main层只读地址配置和严格URL检查。源码环境、配置缺失、配置null、坏配置均有明确行为；不让renderer选任意URL或读文件，不改变ProfileStore。旧离线功能持续可用。

T-d：按S01—S18逐层执行。生成只属于测试的临时CA/证书，Python socket→真实NetworkRoomPort→条件允许时普通Electron两玩家一观众。负例包含坏证书/错域名/过期/明文TLS端口，保持证书验证。缺图形能力时继续其他步骤，不伪造窗口通过。

T-e：保持真实联机行为：连续两场、改时限下一拍、丢连接恢复、服务器重启和停止后的离线单人。汇总原回环160样本与经典192样本，明确N35旧占位和本包窗口独立计数。新失败先在允许文件内修复并回归；越出设计的事项只记录。

T-f：交service-config.example.json（room_url=null）、部署准备说明和PENDING-DEPLOYMENT.md。后者只列实际服务器/域名/证书/区域/成本授权尚待输入，不在本包采购、登录、部署或打新版包。完成后提交，不等用户现场选择主机。

## 可修改路径

- game/server/deidei_server/server.py、__main__.py、新transport_tls.py；仅启动传输相关变化，room.py与核心判定只读。
- game/server/tests/test_transport_tls*.py及README。
- game/desktop/main.cjs、online/service-config.cjs、online/wire.cjs、online/network-room-port.cjs，现有在线提示必要的局部文字及types；不改布局/牌图。
- game/desktop/tests-online/test-tls*.cjs、test-service-config*.cjs；package.json仅scripts，锁和直接依赖版本不变。
- game/integration/secure/**、docs/deployment/r03-secure/**、docs/results/R03-T06-a/**。
- 可选唯一新CI：.github/workflows/r03-secure-prepare.yml，遵守架构A08。旧CI、打包目录、core、runtime、独立样本、旧结果和正式规划均只读。

新文件应被实际require/import和测试使用，不能只放一个无人调用的TLS函数。若需要不同目录先在报告说明，不自行改别人的文件。

## 验收和停止条件

最低交付是参数/配置负例、真实Python和Node TLS链路、原回环与离线回归；窗口项缺环境可PARTIAL。新客户端完整证书验证可行性还需真实Electron证据才能接受，Node成功不能代替。

禁止SSLKEYLOGFILE、NODE_TLS_REJECT_UNAUTHORIZED=0、ignore-certificate-errors、系统信任安装、关闭防火墙/防护；测试root仅以指定子进程启动参数/环境或客户端cafile使用，私钥不上传。成品不能内置测试根。没有主动暴露外网或连接用户未授权服务。

遇到需要系统确认/收费/登录/人工设备的步骤，记录后继续其他获准步骤。本包不保证自动运行到某个时刻，不无限重试。依赖出现新安全告警时保存具体证据，不自动换版本或继续对外交付。

## PR交付

独立worktree与同名分支，先精确stage核对范围，再推送；PR标题`[R03-T06-a] 安全远端连接准备｜提交验收`，base为integration/r03-live。提交源代码后固定tested_code_sha，测试结果另一个提交。报告含S编号、实际L层级、准确源码/规划/工具版本、命令退出码、初次失败、未测、截图来源及所有自有进程退出证明。只读部署资料不能被写成已部署。

用户转交授权本具名分支代码/结果提交和PR；没有合入/发布许可。无需消费T03-d分支，它的测试修订回传后由ChatGPT再安排交叉验收。
