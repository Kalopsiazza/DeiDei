# 第二轮｜任务04｜b｜单人摘要与等待导航修订

ID R02-T04-b · ISSUED · ChatGPT · 2026-09-13。建议继续原任务04线程。

先读[验收](../../reviews/R02-T04-a.md)、[PRD](../../prd/R02-T04-b-PRD.md)、[架构](../../architecture/R02-T04-b-ARCHITECTURE.md)。本次仅两处修订和测试入口维护，不重写游戏。

## 精确起点

- 产品输入：PR #16 的 `980a019d9a1410bf3ee72263d90397023696f32b`，不要从尚未含a包代码的integration分支直接开写。
- 规划文件：本包所在integration/r02的最新规划提交；开始时记录完整plan_sha，只读这些文件。产品input_sha与plan_sha分别记录。
- 新工作分支：`codex/r02-t04-b-live-fixes`，从上述980a019建立独立worktree。不要改a包已提交分支，也不复制规划文件。
- PR目标：`integration/r02`。结果PR会自然包含尚未合入的a包祖先；报告另外给出980a019到b结果的增量文件清单。不得把它误报为自己重写了全部a包内容。

## 工作内容

F01：资源摘要先检查是否生效；取消的攒明确说明未增加DD。已生效的DD变动显示精确玩家单位和中文含义。增加测试，原账目与规则保持不动。

F02：按已给架构暂时禁止开场/切换期间的普通返回导航，成功或失败后恢复。实际窗口复测准备页返回与顶部返回，不只检查pollViews。保留原来的离场、旧响应防护和错误提示。

维护npm test入口，让桌面9项、通信9项及新增回归都自动运行。不得用降低预期或跳过测试处理失败。

## 允许路径

- game/runtime/deidei_runtime/view.py；game/runtime/tests/**；game/runtime/README.md。
- game/desktop/renderer.tsx，必要的新增导航辅助文件，test*.cjs、smoke-live*.cjs，package.json仅测试脚本，README.md。若新增辅助模块需要build.cjs添加构建入口，允许只增加该入口；不改变现有构建含义。
- docs/results/R02-T04-b/**。

其他文件只读。尤其game/core、tests/rules_v1_001、Session/worker业务逻辑、旧源码/模型、依赖版本与lock、CI及所有规划文件。遇到范围外问题先报告具体输入与预期。

## 交付

REPORT.md、TEST-MATRIX.md、MANIFEST.json、PR-BODY.md，附命令/退出码、受测SHA、增量文件、两个发现的前后证据和真实截图。缺GUI能力写PARTIAL；原资料不冒充本包窗口测试。测试档案与个人资料分开。

用npm test、runtime测试、核心174、原独立192以及新增回归验证；有既有基础检查时原样运行。不要运行Forge、换依赖、发布安装包、买证书或关闭防护。Windows同学进度不作为本包前提。

## 提交方法

先检查当前目录未提交工作；使用git worktree add -b codex/r02-t04-b-live-fixes <新的目录> 980a019d9a1410bf3ee72263d90397023696f32b。实际目录由执行者选择，不覆盖原目录。

逐项git add获准文件，检查git diff --cached --check。提交标题带[R02-T04-b]，push到自己的任务分支，再使用：

```sh
gh pr create --repo Kalopsiazza/DeiDei --base integration/r02 --head codex/r02-t04-b-live-fixes --title "[R02-T04-b] 单人摘要与等待导航修订｜提交验收" --body-file docs/results/R02-T04-b/PR-BODY.md
```

自己fork时head使用用户名:分支。读取创建后的URL、base/head和完整SHA；实际成功后才报告已提交。没有授权自动合入、关闭原PR、发下一包或发布。ChatGPT读取结果验收。
