# 第二轮｜任务04｜a｜本地会话与真实单人对局集成

ID R02-T04-a · ISSUED · ChatGPT · 2026-09-13。

## 先读与起点

先读docs/production/R02-LIVE-START.md、docs/reviews/R02-FIRST-DELIVERIES.md、PRD-R02-LIVE、ARC-R02-LIVE。本轮附件内original-r02-input.zip保留classic-1.0.1、CONTRACT-R02和入口映射，SHA为f5e97fe4395f6f3d5552c30b1fe0052563e75c7d7e5d07c97bb78b960fa554b1。它已由规划者写好，只读使用；仓库旧1.0正文不能覆盖它。

从本次已创建的integration/r02起点建立独立worktree和分支codex/r02-t04-a-live。启动时记录完整输入SHA，核对SOURCE-MANIFEST。此分支已包含准确核心、独立测试、桌面源文件，不再让线程自行拼接三个浮动分支。

## 做什么

按PRD L01—L08、架构A01—A07完成Session、随机对手、view、JSONL worker、主进程WorkerPort和真实单人页面循环。修复对手公开资源、通用DD格式、读取失败、自动休整刷新与source提示。一个线程负责集成，避免多人同时改renderer/主进程。

核心及其预期先读后调用，不修改；碰到真实偏差，提交最小输入和实际/预期，让ChatGPT判断。不要为让UI工作再写一套胜负函数。优先简单、可测的实现，不新增网络服务或通用插件机制。

## 允许文件

- game/runtime/**：新的Python标准库实现、测试、README。
- game/desktop/**：只改本轮集成与显示需要的文件；既有依赖版本和lock不变，保留演示入口与原自测。
- docs/results/R02-T04-a/**：报告、TEST-MATRIX、manifest、PR-BODY、日志及真实截图。

只读：game/core/**、tests/rules_v1_001/**、原根源码/模型/测试/依赖、正式规划文档、所有其他结果。不得加入或改CI，不运行Forge分发。源码开发运行需要Python/Node，如实写明，不能称免环境安装版。

## 验证与停止

先跑原独立样本，确认192通过；新增会话S01—S10、协议与显示测试，再打开真实Electron验证。分开记录核心、Session、UI、平台和物理断网；原2份session样本仍由独立线程后续接入复核，不改它们标记充数。

涉及断网、安装和系统授权让Teddy本人处理，先保存可离线查看步骤。不能关闭防护、删测试、加载不可信模型或动私人数据。无Windows不会阻止本包；缺实际图形证据交PARTIAL。

缺依赖/设计疑问写清受影响功能，停止对应工作。禁止自动改规则、换主方案、继续下一包、合入PR或发布。

## 提交

从自己的任务分支提交，PR目标为integration/r02（本轮已经存在），不再投旧docs/design-discussion-20260911。这是本包明确的目标变更，旧a包PR目标不调整。

提交前用git diff --name-only和git diff --cached --name-only核对允许路径，只git add自己的目录。执行git diff --cached --check。提交标题带[R02-T04-a]；然后git push -u origin codex/r02-t04-a-live，并用gh pr create --repo Kalopsiazza/DeiDei --base integration/r02 --head codex/r02-t04-a-live --title "[R02-T04-a] 真实单人对局集成｜提交验收" --body-file docs/results/R02-T04-a/PR-BODY.md。

自己fork时head用自己的用户名:分支。回读PR的base/head/SHA，成功才报告已提交。报告含input_sha、核心/样本/桌面来源SHA、实际tested_code_sha、实际命令/退出码、真实平台、临时对手名称与随机方式、已知问题、截图/日志hash；没有跑的项目写NOT_RUN。
