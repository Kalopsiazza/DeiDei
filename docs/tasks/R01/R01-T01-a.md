# 第一轮｜任务 01｜a｜游戏规则详细确认

ID：R01-T01-a · 版本 1.0 · ISSUED · 作者 ChatGPT  
类型：只读取证；有正式 PRD 后才会发规则实现包。

输入快照：`plan/r01-v1`；结果 PR 目标：`docs/design-discussion-20260911`。开始记录快照实际完整 SHA。先读根目录 WORK_START_HERE.md、AGENTS.md、docs/production/WORKFLOW.md、PRD-R01 1.0、ARC-R01 1.0，再读本包所列输入。不得从讨论记录直接实现游戏，不修改 PRD／架构／计划。

## 本包目标

给 ChatGPT 和 Teddy 一份可以逐项确认的现有规则证据。Codex 不决定高中原玩法、不代写 PRD、不设计新的规则数据模型。

额外输入：`deidei_env.py`、`gui_deidei.py`、`deidei_gym_env.py`、`docs/game-rules.md`、`docs/known-issues.md`、`tests/`。按指定快照读取，记录源码文件行号和提交。

## 步骤与产物

1. 列出 ALL_MOVES 实际目录，与 GUI 中文名对应；若实际数量不是已读的 31 项，先报告差异，不能删项凑数。
2. 在 MOVE-INVENTORY.md 逐招记录代码可用条件、真实资源消耗／收益、次数／延迟／附带状态、可定位源码及旧说明差异。只写证据，unknown 不猜。
3. 执行 `python scripts/check.py`，原样记录通过、失败和 expectedFailure。需要补观察时在实验目录写只读取证脚本；不修改 tests 或原源码。
4. 按 PRD-R01 的问题清单输出 QUESTIONS.md。每题给具体局面、现有证据、需要 Teddy 回答的一句话；最多首批 12 个关键问题，剩余归入附录，避免一口气追问几十次。
5. 输出 CASES.md：普通攻击相遇、资源不足、反弹／自 Bi、炸药时序、张新伟／历强；另列至少三个未定多人局面。对多人只列已有口述能证明的部分，不串用双人函数猜最终结果。
6. 写 REPORT.md 和 manifest.json，创建具名 PR，回报链接。可以在本线程向 Teddy展示待答表并原文保存答复，但正式规则归纳与采纳仍交 ChatGPT。

## 允许写入

`docs/results/R01-T01-a/**`；`experiments/r01-t01-a/**`（取证脚本与 README）。执行分支 `work/r01-t01-a-rules`。禁止其他目录变更；不新增运行依赖、不训练、不修 bug、不重编号。

## 验收／停止

对应 RULE-01—RULE-05。报告说明源文件与模型没有改动；脚本输出有命令和退出码。待 Teddy 回答不妨碍提交 a 包证据。遇到未知就写 OPEN，不填一个“合理规则”。无法运行可提交部分证据并明确环境问题，不把阅读当成测试。

输出完成后状态为 SUBMITTED，等 ChatGPT 验收。b 包由 ChatGPT 根据答案重新签发；不得自行进入游戏实现。
