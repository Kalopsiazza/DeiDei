# 后续部署尚待输入

本文件不表示已部署；以下信息用于下一次明确授权的部署任务。

- 实际服务器：已有主机/服务商、系统、普通运行用户和可用访问权限。
- 域名：预期服务域名及 DNS 管理权限。
- 证书：匹配域名的正式完整证书链、私钥在服务器上的受控路径、签发与续期授权。
- 访问区域：玩家所在地、服务器区域与需要验证的实际设备/网络。
- 成本：已有预算、允许的采购范围和费用上限。
- 操作授权：明确允许哪台主机上的部署、监听地址/端口与 DNS/网络修改。

真实地址与证书确定后另交配置成包和部署验证；本包不采购、登录、签发正式证书或修改系统信任。
