# PRD-R03-LIVE 1.0｜自动集成、持续运行表现与诊断交付

ChatGPT · 2026-09-15。适用R03-T04-a、R03-T03-c、R03-T05-a。继续classic-1.0.1与rooms-1.1，原房间参数已确认。三个PR尚有失败，本批以修复并完成更深验证为目标。

| ID | 交付要求 | 判断方式 |
| --- | --- | --- |
| L01 | 合法请求被限流仍收到可关联的拒绝回执 | request_id精确一致；不调用业务、不消耗命令序号、不修改房间；错误后客户端不继续显示在途等待 |
| L02 | 同一有效截止时刻向所有人表达相同 | 相同阶段/相同服务期限的deadline_at_ms不随同步/重连/广播对象抖动；本机单调时钟判断截止 |
| L03 | 创建、加入、准备、开场、出牌、揭晓、整场结果与再开由真实网络驱动 | Electron主进程使用真实NetworkRoomPort、WebSocket服务与准确核心，不能用MOCK替代 |
| L04 | 六参战者及独立观众都能看见正确状态 | 至少一组六玩家加六观众socket验证；另至少三个真实Electron实例分别为房主/普通玩家/观众 |
| L05 | 已确认房主规则全程一致 | 10秒；下一拍改时限；前三次代理/第四次关闭；after_turn；淘汰留座及管理权限 |
| L06 | 断线、重连、移除、重复及乱序消息不产生重复游戏作用 | 原在途请求重送最多应用一次；旧房/旧连接不覆盖新场；新会话不能读取旧房 |
| L07 | 真实窗口走完失败恢复 | 错房/错密码/满席/观众满/被移除/服务重启/退出超时均有已有设计中的提示或回退；不能新增假赢家 |
| L08 | 无网络或服务不存在时原单人仍能玩 | 原源码与成包单人保持；没有服务时在线入口明确不可用，不误标真实联机 |
| L09 | 建立可反复运行的全链路检查 | 一条命令串行执行已有核心、运行层、桌面与在线检查；独立样本保持独立预期，不能拿产品输出生成答案 |
| L10 | 运行一批重复对局与故障组合 | 状态序列测试默认100场；长期运行默认15分钟、最多四房；性能数字实际测量，不预先宣称目标通过 |
| L11 | 新版原生诊断包带齐文件 | 客户端含在线CJS与离线worker；另有本机服务可执行文件。最终ZIP重展后无需系统Python运行 |
| L12 | 成包真实联机验证 | 一个真实成包Electron加脚本socket同桌；认证、加入、改时限、退出与离线单人均走真实路径；不同系统原生分别记录 |
| L13 | 无人在场时仍能做大部分工作 | 可用环境内自动回归、日志、下载校验及提交；人工事项留待醒后，不进入阻塞式问答 |
| L14 | 产物和证据可定位 | 源版本、规划版本、内部ZIP及外层artifact hash、许可、平台、到期时间、精确启动方式均保留 |

## 玩家行为不新增选择

所有选项沿已确认policy-effective：after_turn、early_reveal=true。时间候选仍5/8/10/12/20/30秒。正式素材和动画不在本批；只允许为既定信息增加必要状态提示，不调整整体审美与牌区排布。六个测试机器人不构成产品新增多人AI。

## 两类不同验证

本机socket和真实Electron证明模块能协同工作；原生CI和包内容检查证明部分系统与交付能力。它们均不等于六位朋友异地对战，不等于Windows普通用户实测、物理断网、系统信任或公网部署通过。这些项目保持原有待验收状态。

## 完成条件

T04：已知三条失败得到解释并修复，至少159个可执行独立样本通过；N35如旧驱动仍列未测，真实窗口结果独立编号，不改该占位充数。至少三个真实窗口完成规定旅程，单人回归通过，才记INTEGRATED_LOCAL_PASS。缺GUI则只能PARTIAL_SOURCE。

T03-c：测试工具/预期可接受，与被测产品通过分开。原三失败不删除，不放宽时间相同断言，不用不相关错误代替限流回执；失败及未测分别报告。只有实际固定候选全部相关检查通过才记对应候选PASS。

T05：先形成可复现的诊断构建，再对通过T04候选做最终ZIP验证。有未修缺陷的基线仅作DIAGNOSTIC_BASELINE，不称联机测试版完成；最终发布判断仍由ChatGPT/Teddy作出。没有某平台时交可执行脚本、实际平台产物和未测说明。
