# PRD-R02-T04-b 1.0｜单人集成小修

ChatGPT · 2026-09-13。沿用PRD-R02-LIVE、ARC-R02-LIVE与classic-1.0.1；只处理验收中的F01、F02与回归入口。

## 目标

玩家看到的资源摘要与实际余额一致；开场等待期间不能通过返回菜单走出页面后，又被晚到的开场结果带回来。保留已经完成的选牌、随机对手、会话、33牌布局和本机档案。

| 编号 | 期望行为 | 接受证据 |
| --- | --- | --- |
| B01 | 攒被云/吸收取消时，说明本次未增加DD；不显示成实际获益 | 真实核心产物进入摘要，检查取消标记、余额及中文文案 |
| B02 | 已生效的DD支出/收益用精确玩家单位显示；原始账目不改 | 普通攒、普通削、吸收、清空与新局归零各有案例 |
| B03 | 开场和切换场景等待期间，页面返回入口不可用，处理函数也防止跳页 | 对延迟成功/延迟失败的startSolo分别测试准备页返回、顶部入口 |
| B04 | 成功后可以正常离开；失败后可以重试或返回；没有一直处于忙碌状态 | 实际Electron操作及对应异常测试 |
| B05 | 标准npm test运行原桌面与通信测试，新增回归同样被默认执行 | 命令、退出码和实际测试清单 |

不改规则数值、输入输出字段、分支选择、会话缓存策略或随机对手；不加入联网、依赖变更、Forge分发、自动更新或正式美术。旧依赖告警继续如实记录。

本次只有一份b包。原工作包获准实现的其他内容保持，新增提案先返回ChatGPT，不增加额外页面设计。
