## 这次想解决什么

桌面单人入口原为固定脚本。本包接入已验收的 classic-1.0.1 核心，使玩家与临时随机对手完成真实选牌、提交、揭晓、结束与再来一场。

## 改了什么，哪些没有涉及

- 新增标准库 Session/随机对手/公开 view/JSONL worker，成功请求只应用一次、过期与冲突拒收，曾义自动休整。
- 主进程 WorkerPort 按进程隔离请求；空闲退出、超时和读取错误显式中断，不隐式重开。
- 两方公开资源及精确 DD 分数、单个在途刷新和旧响应隔离；保留 33 牌三排布局及 fixture 开发预览。
- 核心、原独立样本、规则、旧模型、依赖版本、锁文件和 CI 不变；未涉及联网、训练、Forge、签名或发布。

## 怎样验证

受测代码：`e340a2a20fa8d8274c995b3a17759abf3ade92b6`；输入：`41029218df420985ec06c01f27d4620fd8f35a16`。

- `python3 scripts/check.py`：40 项，39 通过 + 原 #1 的 1 项预期失败。
- 原核心 174、独立工具 8、独立核心样本 192 通过；原 2 份 session 样本仍 NOT_RUN。
- 新 runtime 13、原桌面 9、新桌面/通信 9 项通过；类型检查与构建通过。
- macOS arm64 真实 Electron 自动化完成两场完整实算、重开、自动休整、读取失败重试和 worker 终止；1366×768 / 1920×1080 开发视口截图与实际 ledger 已保存。
- 12 个固定 seed 连续真实规则对局均结束；窗口专用入口固定随机种子，普通产品使用 SystemRandom，不加载旧模型。

命令/退出码、来源 hash、截图、限制见 `docs/results/R02-T04-a/REPORT.md`、`validation.json`、`MANIFEST.json`。

## 对现有内容的影响

- 游戏规则 / 招式编号 / 状态编码：无变化。
- 已有模型 / 新依赖 / 配置：模型不加载；无新依赖，锁不变；源码运行需 Python/Node，新增本机 DEIDEI_PYTHON 启动选择。
- 文档和第三方来源：运行说明与任务结果；桥接参考固定 R01-T02-b 提交，入口映射原样来自原任务合同，署名不变。

## 已知问题

原 23 项开发工具链告警（3 low、19 high、1 critical）未处理。Windows、物理断网、用户手动操作、免环境分发、签名、旧模型未运行；不称为发布验收。原两份独立会话样本保留 NOT_RUN，后续再独立复核。Kimi 未调用，已完成聚焦自审。

## 提交者确认

- [x] 我检查过本次改动，确认 AI 没有混入无关内容。
- [x] 我如实记录了实际验证结果，没有通过删测试或跳过检查隐藏问题。
- [x] 我没有提交密钥、个人资料或无分享权限的材料。
