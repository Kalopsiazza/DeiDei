## 这次想解决什么

为 R02 新核心准备与实现独立的经典规则 1.0.1 输入/预期，防止只校对胜者而漏掉资源、历史、击杀或新局重置。

## 改了什么，哪些没有涉及

- 194 个 JSON 样本覆盖 C001—C082 和 33 个入口，包含参数、连续回合、条件目标、完整账目与事件断言。
- Python 3.11 标准库格式校验器、公开 API 验收驱动、性质检查和临时副本缺陷注入计划。
- 结果报告、覆盖索引、环境、附件与产物 hash、命令和退出码。
- 仅修改 `tests/rules_v1_001/` 与 `docs/results/R02-T02-a/`；不修改新旧引擎、桌面、正式规划或模型。

## 怎样验证

- 样本校验通过：82 组 / 194 样本 / 33 入口，205 份回合预期。
- 8 项工具自检通过，包含故意破坏样本、错误输出、别名、回击关联和重复事件的拒错检查。
- `python3.11 scripts/check.py` 退出 0：16 文件语法通过，40 测试中 39 通过、1 项原有预期失败。
- 缺核心路径实际退出 2 / ENGINE_NOT_AVAILABLE；会话路径退出 3 / SESSION_NOT_AVAILABLE。
- **真实新核心未运行，通过数 0；六项真实缺陷注入未运行，杀死数 0。** 符合 T02 a 包样本先行的范围。
- 受测工具提交：`380ca1fef0cbaac3359c1dc7177bb6d5eea7202c`。代码输入：`3a81daf0f42416ccb73a5a69748655145e6f2f0c`。
- 未运行 GUI、Windows、模型、网络和物理断网验证，本包没有对应实现。

## 对现有内容的影响

- 游戏规则 / 招式编号 / 状态编码：未修改。样本采用附件 classic-1.0.1 与 CONTRACT-R02 1.0。
- 已有模型 / 新依赖 / 配置：无修改，无新依赖。
- 文档和第三方来源：正式规划只读；ZIP 36 项 hash 核验通过，规划正文不复制进结果。

## 已知问题

等待规划者指定已验收核心 SHA 后实际联测。C074 房间记录与 C081 应用去重等待会话适配器；C075 明确为有限域探针，不宣称穷尽任意状态。未调用 Kimi，已做本地自审。

结果目标为 `docs/design-discussion-20260911`，提交验收；尚未合入，真实核心联测等待规划者后续工作包。

## 提交者确认

- [x] 已核对本次范围，没有混入其他任务内容。
- [x] 区分了样本校验、自检、旧测试与尚未运行的新核心，不删测试、不新增 skip/expectedFailure。
- [x] 未提交密钥、个人资料、模型、附件规划正文或无分享权限的材料。
