# 给 ChatGPT Pro｜R01-T02-b 验收与 Windows 独立任务请求

## Teddy 本次明确授权（优先于附件中原两端验收门槛）

Teddy 在当前任务中明确说明：

> 我有一个朋友可以帮我在 Windows 端验证，但我没有 Windows 环境。为了不影响开发，授权在未取得 Windows 端测试成功的情况下进入下一步。请 ChatGPT 单独派发一个完整的 Windows 验证任务，交给使用 Windows＋Codex 的同学，尽量由他的 agents 工具完成，最后由他提交 PR；他的进度不能阻挡我们第二轮进度。

这是**接受 Windows 验证延期的推进授权**，不是 Windows 已通过的证据。请将 Windows 的构建、实机、离线、无系统运行时、签名安装逐项保留真实状态；不要把 CI 通过写成同学实机通过。第二轮由 ChatGPT 按其他前置条件独立规划与发包；本执行线程不自行启动 R02，也不更改总进度、PRD 或架构。

## 请另发一份完整的 Windows＋Codex 工作包

以下是供 ChatGPT 正式编包的交接要求，不是本线程擅自签发的新编号任务。建议覆盖一次完整的“取包—构建—回归—实机—异常—证据—PR”工作，不要只让同学点几次按钮。

1. **固定输入与可独立开始**：明确被测完整代码 SHA、Windows x64 ZIP 的 artifact/run ID、SHA-256 和失效时间。artifact 尚缺或过期时，给定同一代码的原生构建步骤；即使第二轮已推进，也不让同学追逐浮动分支。其任务不以等待或修改 R02 代码为前提。
2. **本机环境与可复现构建**：记录 Windows 版本／CPU 架构、Node/npm/Python/uv 版本；使用本包 exact npm lock、Windows Python 锁定清单，原生构建 PyInstaller worker 与 Forge 应用，核对 package-manifest。依赖取不到时记录具体版本和错误，不私自换一套环境并冒充原测试。
3. **进程与协议回归**：跑既有 32 项基础检查、新实验回归及打包 worker 检查；覆盖旧代晚到 exit/stdout、失败 spawn/管道、重复 stop、协议错误快速重试、并发单次完成、200ms 与真实 10s 超时、进程回收。已知原规则 expectedFailure 原样保留。
4. **Windows 分发实机**：下载包与自己构建包分别记录；从含中文／空格目录普通双击 exe，六席位及本地素材、档案读写及重开保持、四固定案例、seed 可重复 Easy 都要测。确认运行的是包内 worker.exe，而不是系统 Python。十次完整启停记录本程序及 worker PID，不能误杀其他程序。
5. **真实离线与干净环境**：同学亲自断开网络，先退出后重开；记录网络状态和测试时间。没有系统 Python/Node 的独立用户环境、虚拟机或测试机单独验证，限制 PATH 不能替代该项。虚拟机证据与物理笔记本证据分开标注，不伪造“纯净系统”。
6. **异常与安全提示**：损坏测试档案、只读测试目录、非法参数、worker 失败、恢复重试可见性；只用专用临时测试数据。保留 SmartScreen／杀毒拦截的原文和截图，不关闭保护、不删除安全标记绕过。无权限或工具不能操作时，由同学做所需人工步骤并注明。
7. **测量与汇总**：程序启动耗时、进程内存、AI 往返时间、包大小，逐项说明机器与计时口径；失败给最小复现、预期／实际、命令、退出码、截图和受影响范围。不能只发“都正常”。
8. **交付 PR**：在同学自己的 fork／独立 worktree 与功能分支工作，由 ChatGPT 指定独立结果目录和目标分支；原规则、原模型、a/b 实验与 R02 文件原则上不改。验证发现需修复时按新包允许范围提交最小改动，不吞入无关重构。最后发 PR，附 REPORT、TEST-MATRIX、manifest、RUNBOOK、环境清单、截图／日志、包 SHA 和 artifact 入口；不得自行合入。

## 尽量使用同学的 agents 工具

建议由同学的主 Codex 负责环境安全、固定输入、集成与 PR；在工具确实可用时，安排可独立运行的有界子任务：

- agent A：构建、依赖链及可复现性；
- agent B：进程／协议与异常回归；
- agent C：检查测试矩阵和证据对应性，独立复核没有把自动化当人工验收。

原生窗口操作由一个执行者串行控制，避免多个 agent 同时点击同一窗口；不要并发修改共享环境或同一文件。没有多 agent 能力时由主 Codex 顺序完成，记录实际工具，不能假装使用。网络切换、系统安全提示、需要真人判断的项目由同学亲自处理，其他可自动完成的安装、测试、采样、报告、Git/PR 尽量交给 Codex。

## 与第二轮并行，不设等待门槛

- Windows 任务独立提交、独立验收；第二轮不以它的完成时间或 PR 合入为前置，不将它的检查加入 R02 必须通过的 gate。
- ChatGPT 应把“Windows 验证延期，Teddy 已接受”写入下一轮前提，保留后续收敛事项。
- 如同学发现影响共有协议或数据保存的严重问题，提交具体证据供 ChatGPT 按影响判断；不要让普通 Windows 环境排障、签名提示或测试进度自动拖住整个第二轮。

请先审查本包真实修复与证据，再按照上述授权处理延期项并发独立任务。未调用 Kimi。
