# R03-T01-b 自测映射

服务测试继承 a 的 42 个方法，新增 19 个方法；N 编号并非 unittest 方法总数。
本文件描述本任务自测，独立 T03、桌面连接及真人验收不混入 PASS。

| 编号 | 本轮覆盖 |
| --- | --- |
| N01–N25 | 原建房、密码、容量、保密、权限、幂等、选牌、超时、核心与弃权回归原样保留；N01 继承夹具显式设 12 秒；默认 10 秒由 N37 验证 |
| N26 | 按 P20/P22 改为 after_turn 主动退出、第四次缺席关闭；不再使用旧第三次关闭预期 |
| N27 | 按 ARC1.1/P22 替换全场暂停预期：select/reveal 连续计时、重连不改截止，非参战宽限 0/30/60 秒 |
| N28–N34 | 原断线、重连、跨房、慢 writer、容量、安全、C065 回归保留 |
| N35 | NOT_RUN：桌面联机回退属于 T02/T03；离线桌面基线另记 |
| N36 | 六个真实 socket 与两观众连续三场、真实时钟 5 秒截止 |
| N37 | rooms-1.1 hello、默认 10 秒与时限枚举 |
| N38–N39 | 房主权限、同值只 ACK、原请求重放、新请求 stale、当前截止/令牌/提交/准备保持，下一拍生效，大厅/select/reveal/result 修改 |
| N40 | 在线/离线房主前三次固定 Charge，第四次 resolver 调用为零，不新增结果 |
| N41 | 已提交后断线、重连不清计数、成功人手提交清零；在线/离线强制休整、第四次不调用核心 |
| N42 | 淘汰房主留座与改时限，其他人继续；结果阶段起算宽限、重连取消；大幅推进时钟按最近截止处理 |
| N43 | after_turn/immediate × 大厅/select/reveal/result；含淘汰房主，计数核心调用，揭晓不重算；既已公布的 result 保留 |
| N44 | 弃权后 0/1/2 存活者，to_game_id 为字符串且等于 effective_state.game_id |
| N45 | 在线移除无需新命令即收到定向通知；resume ACK 后相同回执；四类非参战断线释放 |
| N46 | 进入新房后旧 ACK/回执不恢复旧资格；阻塞 writer 检查排队回执、新会话世代隔离 |
| N47 | 非法／缺失 UUID 错误 ACK 精确形状、有效 UUID 保留、房主同角色切换 HOST_ROLE_FIXED |
| N48 | 六名玩家＋两观众，动态改时限及房主代攒，真实核心连续三场 |
| N49 | Unicode 码点长度、空白昵称、Cc/Cf/Cs、合法空格昵称不 trim；密码边界与控制字符 |

所有网络场景均为真实 loopback WebSocket；大多数使用可注入时钟，部分初态／休整状态使用核心工厂。
强制休整第四次边界在测试内部注入计数，不是对外 API；慢消费者使用 writer 阻塞，非物理弱网压测。
N01–N36 详细继承定义见只读 `../R03-T01-a/TEST-MATRIX.md`，版本变化以上表为准。
实际 PASS 与命令以最终 MANIFEST 和 evidence 为准；不以本表代替运行证据。
