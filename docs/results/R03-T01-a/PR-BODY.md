## 这次想解决什么

交付 R03-T01-a 好友房服务及本地自测，并记录用户最新参数答复，供 ChatGPT Pro 修订工作包。
**本 PR 为草稿：现有实现符合旧 a 包，新要求尚未实现，不能按新要求合入验收。**

## 改了什么

仅增加 game/server 与本任务结果：临时身份/重连、房号密码、角色准备、权威截止、多人核心结算、
超时/休整/弃权、私有快照、幂等、消息/队列边界，以及 A08 真 socket 测试入口。
未改 core、runtime、desktop、旧测试、打包、CI 或正式规划。

## 用户答复与待 Pro 修订

- 已确认：淘汰玩家保留参战席位，与独立观众分开；提交后不可改；普通玩家当拍结算后离开。
- 默认改为 10 秒，房主可在游戏中改时限。当前代码仍为 12 秒、仅建房配置；需要协议、页面和计时规则修订。
- 房主掉线自动出三轮 deidei、第四次仍超时再关闭。当前代码为暂停 30 秒/第三次缺席关闭，需要统一修订。
- 房主主动离开是否也延后到当拍结束、第三/第四次阈值的区别待一次澄清，不自行猜定。

详见 docs/results/R03-T01-a/CHANGE-REQUEST-FOR-PRO.md 与 DECISIONS.md，JSON 保留用户原话。

## 怎样验证

固定服务代码 417ae52f50fb806d1205381a4a148bc96117c7d6；macOS arm64 / Python 3.13.7。

- 服务 42 tests 通过，含六客户端加两观众连续三场及真实时钟截止。
- 核心 174 tests、独立核心 192 fixtures / 469 calls、工具 8 tests 通过。
- 根检查 40 tests：39 通过 + 1 原有 expectedFailure；pip check、CLI help 通过。
- 本次答复更新仅为结果文档；服务源码与受测提交相同，检查 JSON 和变更范围。
- 独立房间 T03、Electron、Windows、跨机、真人 NOT_RUN；慢 writer 和容量测试有明确注入边界。

## 影响与已知问题

仅新增独立 websockets==17.0.1 hash lock，专用环境安装；无规则/模型改动。
当前测试不代表新意向已实现。请 Pro 审阅差异并下发修订包，不自动合入或发布。
未调用 Kimi；Codex 自查完成。

## 提交者确认

- [x] 变更限于获准目录。
- [x] 验证和未测项目分别记录，无降低既有断言或新增 skip。
- [x] 无凭证、个人材料、运行时环境或大文件。
