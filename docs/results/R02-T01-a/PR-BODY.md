## 这次想解决什么

按 R02-T01-a，从零提供 classic-1.0.1 的纯 Python 裁判：2—6 人同时出招后，保留完整作用与账目，再决定继续、新局或整场结束。旧两人 `simulate_turn` 不承担新版结果。

## 改了什么，哪些没有涉及

- `game/core/` 实现合同规定的三个公开调用、33 入口/26 完整实际招式、严格 wire 校验和 P1—P4。
- 复合招按本人加固定招式玩家的局部视图试算，正式全桌恢复后不重选；回击保留原值与原攻击来源，死亡不取消同轮效果。
- 完成炸药、曾义、复制历史、一次费用、存活者重开；完整结果与下一状态隔离。
- 标准库自测、README 示例和本包结果记录。未改旧源码/模型、根配置、正式规则、T02 样本或桌面/运行层。

## 怎样验证

受测代码：`002e3882b91e64f0204f90e24a3e6ee0f783ef23`。macOS 27.0 / arm64 / Python 3.11.15。

- `PYTHONPATH=game/core python3.11 -m unittest discover -s game/core/tests -v`：174 项通过，无 skip/expectedFailure。
- `python3.11 scripts/check.py`：17 个文件语法通过；旧 32 项中 31 通过、1 原有 expectedFailure 保留。
- README 公开调用示例：退出 0。输入附件的 36 份文件 SHA256 和 33 入口映射核对通过。
- 新测试覆盖 C001—C082 的核心部分；C074 房间信息和 C081 会话应用去重未运行。证据、完整入口/规则覆盖和产物 hash 见 `docs/results/R02-T01-a/`。

## 对现有内容的影响

- 新增新版规则实现，未修改旧招式编号、状态编码、模型和旧测试。
- 无运行依赖新增，`pyproject.toml` 仅局部元信息。
- 使用交接包指定输入提交 `3a81daf0f42416ccb73a5a69748655145e6f2f0c`；正式规划附件保持只读，不复制入 PR。

## 已知问题

无已知未实现的本包招式；自测不是独立验收，等待规划者对固定提交运行 T02。真实 worker/AI/桌面、安装包、跨平台、会话去重和外部审查均未做；Kimi 已暂停且未调用。未合入或发布。

## 提交者确认

- [x] 已检查差异，仅包含本任务允许路径。
- [x] 记录实际命令、退出码与受测提交，没有删测试、改规范或新增跳过来掩盖失败。
- [x] 不包含密钥、私人材料、模型副本或虚拟环境。
