## 这次想解决什么

修复真实联机中的限流回执关联和公开截止抖动，并把既有服务、桌面和核心串成可复跑的真实窗口回归。

## 改了什么，哪些没有涉及

- 有界解析后保留UUID；达到第五次违规时有界发送最后一份拒绝，再按原策略关闭。
- 单调时钟固定锚点投影全部公开时间，保持原截止/规则/消息形状。
- 默认npm test含25个原桌面和28个在线测试；新增一条集成总控、三窗口真实网络、固定序列和四房持续运行脚本。
- 只改game/server、game/desktop脚本、game/integration和本任务结果；core/runtime/独立预期/依赖锁/旧打包/CI未改。

## 怎样验证

候选 **d0c96408a3aa8c14174099da4247bcac953fb9f7**；起点 **8a6f8b29f517ab4c5a16466f0894e86995f8a852**，规划 **a1e01ee259c65d9241ad1c4daee7852faca366bf**。

- 服务66、核心174、runtime23、独立核心192案例/469次结算、独立工具8、rooms工具19、桌面53通过；根59项（58通过、1历史预期失败）。
- 原独立160例完整复测三次：每次159 PASS + N35 NOT_RUN，无FAIL；修复前156/3/1和中间158/1/1原始输出保留。
- 普通Electron main/preload/NetworkRoomPort接真实CLI服务，三个窗口、6+6、三场完整结果、改时限、ACK丢失重连、自动移除、主机丢失、服务重启、离线单人及关闭等待验证通过；1366×768和1920×1080真实截图保留。
- 100固定seed通过；最终真实钟持续900.053秒，4房48连接完成636场/1913回合；48连接全部关闭、服务退出。响应时间包含客户端55ms节流，不作性能KPI。
- 复跑：`/path/to/venv/bin/python game/integration/run.py --output /tmp/deidei-integration-result`。实际命令/退出码/哈希见docs/results/R03-T04-a。

## 对现有内容的影响

无经典规则、模型、依赖版本或安全设置变更。源码候选，未部署、合并或发布。
CANDIDATE.json严格指向先前代码提交，可供T03-c/T05自动读取；候选通过不代替它们的独立验收。

## 已知问题

N35旧驱动占位仍NOT_RUN；真实GUI另按Q编号，不把MOCK当联机。Windows、物理网络、跨电脑真人和最终ZIP不在本次通过范围。原生对话框的取消/确认由测试脚本自动选择。未调用Kimi。

## 提交者确认

- [x] 范围、受保护tree和依赖锁已核对。
- [x] 未删除独立失败或降低断言，首次失败与未测分别保存。
- [x] 未提交凭证、临时档案、运行包或私人材料。
