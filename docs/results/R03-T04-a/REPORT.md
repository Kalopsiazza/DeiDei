# R03-T04-a 真实联机集成与自动回归

当前：候选已达到 A10 的交接条件，最终重复验收与持续运行仍在执行。
产品起点 `8a6f8b29f517ab4c5a16466f0894e86995f8a852`；规划固定 `a1e01ee259c65d9241ad1c4daee7852faca366bf`。
候选代码 `d0c96408a3aa8c14174099da4247bcac953fb9f7`，CANDIDATE.json 单独提交，绝不以自身报告提交代替 code_sha。

## 实现

- 服务先做有界严格解析与 UUID 识别，再限流，拒绝不消费业务序号或缓存。第五次违规的最终拒绝经同一 writer 发出后关闭，等待最多1秒；原五次阈值、1008码及队列预算保持。
- 一个服务实例固定 mono/wall 锚点，所有公开时间同一投影；实际到期只按单调时钟。系统 wall 前后跳动不改变选择、揭晓或房主宽限。
- 桌面 package.json 只改脚本，默认 npm test 包含原25+在线28。生产 main、preload、NetworkRoomPort 已能协作，没有为测试复制产品逻辑或改安全设置。
- game/integration 提供总控、真实三窗口、受控真实消息 relay、固定 seed 序列及四房真实钟运行。测试代码不进产品运行/成包路径。

## 当前验证

固定输入原独立160例：156 PASS、3 FAIL、1 NOT_RUN；中间版本158 PASS仍暴露末尾ACK清理竞争。首次失败和具体修复见 FIRST-FAILURES.md。
最终代码首轮：159个可执行独立样本通过，原N35保留未测。其余两轮在固定检出继续。
服务66、核心174、runtime23、独立核心192/469、工具8、rooms工具19、桌面53均通过；根59项为58 PASS+1历史expectedFailure。
最终100固定seed序列与真实GUI通过。GUI走正常源码 main/preload、真实服务与核心，三场完整结果再开，六玩家加六观众，两个尺寸33牌三排。
图片已实际查看；故障 relay 只延迟/丢弃真实服务消息。原生关闭确认的返回值由脚本选择，非用户物理点击。详细子例和模块/窗口边界见 TEST-MATRIX.md。

最终900秒持续运行尚未结束，完成后更新本报告；不把被源修复取代的旧运行计为通过。
复跑命令及前提见 RUN.md；每个阶段真实命令、退出码、平台、源码哈希见 MANIFEST 和 evidence。

## 范围与限制

正式核心/runtime/独立预期/npm锁/server锁/旧打包/CI均未改，package.json除了scripts字节等价。参数沿已确认 after_turn / early_reveal=true，不再问用户。
本任务调用原独立样本，不代表独立线程已验收本候选；其已发布F05/F06基线发现与本轮复现一致。
N35旧驱动没有接窗口，真实GUI按Q编号单独记录。Windows原生、跨电脑/异地真人、物理断网、签名信任及最终ZIP未测；没有部署、合并或发布。
未调用Kimi。当前产物是本地集成源码候选，不是正式分发包。
