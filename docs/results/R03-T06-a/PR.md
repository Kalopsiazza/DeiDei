# [R03-T06-a] 安全远端连接准备｜提交验收

目标 base：`integration/r03-live`。此文件为本地草稿，未创建远端 PR。

## 这次想解决什么

原好友房只能使用回环明文地址。本次增加可选 TLS 服务和桌面只读服务地址配置，为后续受控远端部署准备；默认回环与离线单人持续可用。

## 改了什么，哪些没有涉及

服务启动前验证证书/私钥及远端许可；桌面主进程严格校验 ws/wss 与固定配置文件，保留完整证书验证。
加入参数、配置、真实 Python/Node/Electron TLS 正负例与只读部署资料。规则、房间事务、依赖版本、打包与正式规划不变。

## 怎样验证

- `python scripts/check.py`：59 tests，原已知 expected failure 1 项，exit 0。
- 服务 70、核心 174、runtime 23、经典 192 样本通过。
- 默认桌面类型检查/构建通过，测试 58 PASS / 1 FAIL：旧测试要求拒绝全部 wss，与新需求冲突；尚待允许更新该清单外断言。
- 真实 Python/NetworkRoomPort/普通 macOS Electron TLS 通过。坏 CA/SAN/过期证书未触发身份请求；两玩家一观众两场、改时限、重放、断线恢复、服务重启及持续停止后离线通过。
- 独立房间样本 159 PASS / 1 NOT_RUN（N35），exit 0；进程清理、SHA、命令、截图见 `docs/results/R03-T06-a/REPORT.md` 及同目录证据。
- Windows、新安装包、公网/跨电脑未验证。

## 对现有内容的影响

- 游戏规则 / 招式编号 / 状态编码：无变化。
- 模型 / 新依赖 / 依赖版本：无变化。
- 配置：新增可选 TLS 参数、成包只读 `service-config.json` 支持；正式文件与部署仍待后续任务。

## 已知问题

默认桌面套件旧 wss 拒绝断言待调整；不能以新增 TLS 检查通过代替默认套件全绿。
没有推送、合入或部署。未调用 Kimi，已聚焦自审。

## 提交者确认

- [x] 改动范围已核对，没有混入无关文件。
- [x] 保留首次失败和未测项，未删除测试或降低安全检查。
- [x] 未提交私钥、凭据或真实个人档案。
