# 第三轮｜连续执行批次 1.0

ChatGPT · 2026-09-15。用户要求本批工作量更完整，减少现场问答；不做画风、图片、人物或音效选择。三个包已经写明连续步骤，完成一个步骤后直接进行下一个，不逐项等待 Teddy。

## 当前证据与固定起点

PR #23 服务 b40996e833297e24817b9c24b7830ed2941d9c3d；PR #24 桌面 1dbe1e4f8de28410dc42946efddffc011966cc3a；PR #25 独立测试 8711e75a5710a92f5dcd2eb005b20c1de8da9ce0。

三份目录原样放入产品起点 `8a6f8b29f517ab4c5a16466f0894e86995f8a852`，见根目录 R03-LIVE-SOURCES.json。这个起点保留已知失败，状态为 ASSEMBLED_NOT_ACCEPTED，不等于合入原 PR 或游戏验收通过。新目标分支是 **integration/r03-live**；旧 integration/r03、main 和原 PR 不动。所有执行线程从上述完整 SHA 新建自己的 worktree。

前序独立线程报告160例：156通过、3失败、N35未运行；另11个socket到桌面解码场景通过。三份失败是一个限流回执关联问题、两个真实时钟截止显示不稳定的问题。当前规划者已读相关代码及原始答复，没有在本轮重跑全套，不能把原作者数字写成规划者独立通过。

## 三个包

| 包 | 负责人建议 | 主要产物 |
| --- | --- | --- |
| R03-T04-a 真实联机集成与自动回归 | 原任务01或熟悉全项目的线程 | 修两类缺陷，连真实服务与窗口，执行端到端回归，交可供自动复测的固定产品候选 |
| R03-T03-c 独立长期运行与故障验收 | 原独立测试线程 | 保留160例，增加状态序列、消息保密、断线恢复、限流及队列证据；自动读取T04候选复核 |
| R03-T05-a 原生诊断包与成包联机验证 | 原打包线程或原任务02线程 | 本机服务诊断包、含在线模块的两端客户端包、对最终ZIP的实际自动运行记录 |

产品目录只由T04改；独立样本只由T03-c改；新打包目录与其具名CI只由T05改。三个包有各自完整工作链，缺少另一个结果时先做不依赖它的部分，不让一个线程的等待耗光整晚。

## 不再问同一批选项

已读PR #23的DECISIONS.md：用户回复“按这个”，确认 after_turn 和 early_reveal=true。其余采用已写的 rooms-1.1：默认10秒、六位参战加六位独立观众、改时限从下次选择开始、房主前三次代攒第四次关闭、普通玩家第三次当拍后移除、淘汰留座、非参战房主断线30秒管理性恢复。本批没有待用户回答的玩法题。

本批实现细节按[PRD](../prd/R03-AUTOMATED-v1.md)与[程序设计](../architecture/R03-AUTOMATED-v1.md)。详细旧规范在附件original-r03-b-input.zip及其内原a附件；规划只读。现有rooms-1.1消息形状不扩展；本次只有补明时间与错误关联含义。

## 连续执行办法

每包按内部编号连续做，失败先在自己获准的文件中修正并复跑。保留首次失败和最后证据，不能只交“做完第一步等回复”。遇到不同步、缺图形环境或工具下载失败，把对应步骤记PARTIAL，继续其余工作；不能伪造通过。

无需唤醒用户的事项：已有设计内的代码修正、测试输入、日志整理、临时测试档案、指定本机端口、允许目录内的构建脚本。需用户参与的设备/真实断网/证书/收费/登录/权限确认，写入PENDING-HUMAN.md暂缓，本批继续自动工作。

不部署公网，不开外网监听，不买服务器，不装系统服务，不关闭防护，不使用私人档案或已有房间。不做等级、解锁、多人产品AI、规则更改或新美术。自动测试玩家只存在于测试进程。固定依赖不变；新安全告警保留并停止相关分发，不自动换版本。

## 候选自动交接

T04在分支codex/r03-t04-a-live-integration上提交产品后，另一个仅记录提交加入 docs/results/R03-T04-a/CANDIDATE.json。采用本附件CANDIDATE.schema.json，code_sha指前一个准确产品提交，never指自身未完成的报告提交。

T03-c/T05只读取这个具名分支的一份候选，验证code_sha是本批起点的后代、获准路径、固定core/runtime/依赖内容，再在自己的临时只读检出中测试。不能执行回执里提供的任意命令、URL或文件路径。最多在自身工作开始、独立内容完成后、最终交付前各查看一次；每次最多一次候选，不持续轮询。

若拿到更晚的候选，可再跑一遍，默认最多两份候选；没有候选时不无限等待：交BASELINE_ONLY和一条可复跑命令。基线本来有失败，不可因缺修订写PASS。T04也可读取T03-c的FINDINGS.json；证据与修法符合本设计时继续修本包，不为每个小问题另等工作包。其他规则含义不自行决定。

## 提交与最终报告

每个结果PR目标integration/r03-live；自己的分支见任务正文。先逐文件stage，检查允许路径与差异，不stage整仓，不强推，不改其他线程结果。用户转交本包授权在这些具名任务分支提交、推送和创建PR，不包含合入、关闭原PR或发布Release。

报告写清input_sha、plan_sha或规划清单hash、tested_code_sha、对应工具版本、实际命令/退出码、失败/未测、每份图像是MOCK还是真实服务。精确源提交与测试材料分开。长日志只保存安全摘要与有限尾部；原始大日志用内部artifact，不把会话token/密码/未揭晓动作/用户路径发到GitHub。

完成本包全部获准步骤后提交，不无限运行。执行时环境权限或工具可用性无法保证；最终如实说明实际停止位置。ChatGPT本次交付任务，不在后台启动这些线程。
