# 给编码 AI 的说明

本文件适用于整个仓库。用户的具体任务和本仓库约定一起决定本次工作；遇到玩法含糊或来源不明的内容，报告疑问，不自行猜定。

## 开始前

阅读 `README.md`、`CONTRIBUTING.md`、`docs/development.md`，涉及规则时再读 `docs/game-rules.md` 和 `docs/known-issues.md`，涉及模型时读 `docs/model-card.md`。先检查 git 状态、当前分支和上游最新 main；保留已有未提交工作，不执行会丢失其他人内容的命令。

普通贡献在自己的 fork 和独立功能分支完成。无需先申请 Issue。先说明本次目标、涉及文件和验证方法，再按最小可运行范围开发；没有排期或分工可供猜测。

## 实现

- 延续附近代码的风格，不进行无关重写或全文件格式整理。新 Python 代码使用 4 空格，公共函数补充类型提示；注释重点解释原因。
- 一次 PR 只做一个可独立检查的事项；新依赖说明用途，不改动本次无关的依赖版本。
- 回合计算逐步采用相同输入得到相同输出、且不修改输入状态的写法。现有对象和类可以保留，无需把整个项目改写成某一种编程方式。
- 界面、网络收发、规则计算、AI 策略各自承担自己的工作。新界面调用已有规则逻辑，不复制一套判定；模拟计算不引入界面、联网或文件操作。
- 不擅自改变 DD 单位、招式编号、动作顺序、状态编码、胜负规则或已有模型文件。确有任务需要时单独提交，交代兼容性和迁移方法。
- 联机由服务端判定结果。本回合揭晓前，不向对手发送已提交招式；服务端验证动作、回合和资源，防止重复结算。超时、断线与重新连接的行为写入本次方案和测试。
- 随机策略使用可注入的随机数发生器。测试固定种子，避免把难以复现的随机胜率当作通过标准。
- 保留原作者署名；不自行添加、替换或宣称已有开源许可证。模型文件不自动加载不可信来源的 pickle 或其他可执行序列化内容。

## 验证

从仓库根目录运行 `python scripts/check.py`（必要时使用 python3）。这会检查 Python 语法并运行标准库测试。规则修改增加对应对局案例；界面修改实际打开并附截图；模型修改另做加载检查与可复查评测；网络修改增加信息保密、重复提交和断线案例。

不要删测试或降低断言来消除报错。不要给新问题加 skip / expectedFailure。现有 #1 的预期失败测试已经注明来源；修复后去掉标记，保留断言。把实际运行的命令、结果、未运行内容和原因分开记录。基础检查通过不等于 GUI、模型和联机均可用。

## 交付

使用 `.github/pull_request_template.md` 整理中文说明：解决什么、改了什么、如何验证、已知问题、是否改变规则/模型/依赖。文档只更新与任务有关的部分，不要求另写长篇过程报告。

只提交相关文件；不要提交 .venv、.env、凭据、日志、私人材料、临时训练结果或无关生成物。不要强制推送上游，不自行改仓库权限、发布正式版本或合入 main。维护者明确委托的专门任务按其授权执行，并验证最终状态。

需要维护设置时阅读 `docs/maintainer-setup.md`。GitHub 管理设置与仓库文件是两回事；没有实际调用并回读成功时，不宣称保护已经启用。
